<div class="game">
  <div id="players-desk-1round">
    <% participations.order(:id).each do |participation| %>
      <div id="<%= dom_id(participation) %>">
        <%= render "games/desk", participation: participation, winner: false, looser: false %>
      </div>
    <% end %>
  </div>
</div>
