<% players.each do |player| %>
  <%= player.email %>

  <% if user == player %>
    <%= link_to "Are you ready", ready_participation_path(player.find_participation(game)), data: { turbo_method: :post }, id:"test" %>
  <% end %>
<% end %>
